type User @model @auth(rules: [{allow: owner}]) {
  id: ID!
  username: String!
  firstName: String
  lastName: String
  bio: String
  email: String
  phoneNumber: String
  image: String
  messageCount: Int
}
